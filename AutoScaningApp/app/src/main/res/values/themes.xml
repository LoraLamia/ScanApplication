<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AutoScaningApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="CustomButtonStyle" parent="Widget.Material3.Button">
        <item name="android:textColor">#FFFFFF</item>
        <item name="android:elevation">6dp</item>
        <item name="backgroundTint">@null</item>
        <item name="android:background">@drawable/button_background</item>
        <item name="cornerFamily">rounded</item> <!-- For rounded corners -->
        <item name="cornerSize">30dp</item> <!-- Corner radius -->
    </style>

    <style name="Theme.AutoScaningApp" parent="Base.Theme.AutoScaningApp" />
</resources>